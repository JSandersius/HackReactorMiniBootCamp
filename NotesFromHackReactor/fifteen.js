var numbres = [1, 9, 27, 5, 10, 3];
var sum = 0;

var product = 1;
var maximum = numbers[0];

for (var i = 0; i < numbers.length; i++) {
    sum = sum + numbers[i];
}
}

each();

//for loops are for arrays, in order to make an abstract funtion to iterate over an array, a functino woudl take in an array as a parameter